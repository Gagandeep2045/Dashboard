import { FilterReportByJobPipe } from './filter-report-by-job.pipe';

describe('FilterReportByJobPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReportByJobPipe();
    expect(pipe).toBeTruthy();
  });
});
